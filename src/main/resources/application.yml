server:
  port: 8086

spring:
  application:
    name: MONOLITH-SERVICE
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/DoctorApp
    username: postgres
    password: Password@123
    driverClassName: org.postgresql.Driver

